version: "2"
services:
  underarmour-chat-service:
    env_file: .env
    command: yarn start
    build: .
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    ports:
      - "17688:17688"
